{
  "name": "splaylist",
  "version": "0.0.0",
  "description": "Fast splay tree with order statistics. As fast as a linked list for insertion, deletion, and traversal, but much faster than a list for indexed access and running counts.",
  "main": "src/splaylist.js",
  "keywords": [
    "random",
    "seed",
    "crypto"
  ],
  "scripts": {
    "test": "node_modules/.bin/mocha",
    "coverage": "mkdir -p out; node_modules/.bin/mocha -r blanket -R html-cov > out/coverage.html",
    "server": "node_modules/.bin/http-server out"
  },
  "config": {
    "blanket": {
      "pattern": "src"
    }
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/davidbau/splaylist.git"
  },
  "author": "David Bau",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/davidbau/splaylist/issues"
  },
  "homepage": "https://github.com/davidbau/splaylist",
  "devDependencies": {
    "blanket": "latest",
    "http-server": "latest",
    "mocha": "latest"
  }
}
